It's important to be able to test this extension e2e.

We will need a script that takes an audio file, a nickname and a URL to a public talk room in a nextcloud instance.
It should connect to the nextcloud talk room and stream the audio.
A human user in the room could fire the script multiple times in parallel to check the transcriptions quality and the server load.

A second pass can automate all this.
